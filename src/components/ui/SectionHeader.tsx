export function SectionHeader({ index, title, detail }: { index: string; title: string; detail?: string }) { return <div className="flex items-end justify-between border-b border-[var(--color-line)] pb-[var(--space-1)]"><h2 className="m-0 font-[var(--font-display)] text-sm uppercase tracking-[0.16em] text-[var(--color-information)]"><span className="mr-2 text-[var(--color-primary)]">{index}</span>{title}</h2>{detail && <span className="text-[0.625rem] uppercase tracking-[0.14em] text-[var(--color-muted)]">{detail}</span>}</div>; }
