"use client";

import type { CameraSceneState } from "@/types/three";

export interface CameraRigProps { state?: CameraSceneState; }

export function CameraRig(_props: CameraRigProps) { return null; }
