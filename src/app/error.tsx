"use client";
export default function Error({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <div className="grid min-h-[70dvh] place-items-center p-8 text-center"><div><p className="text-xs uppercase tracking-[0.18em] text-[var(--color-danger)]">System fault // 500</p><button onClick={reset} className="mt-4 border border-[var(--color-line)] px-4 py-3 text-xs uppercase tracking-[0.12em] text-[var(--color-primary)]">Retry command</button></div></div>; }
